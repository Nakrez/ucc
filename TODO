-- PARSE --

- Add C99 features
- Check strings/char/floats

-- AST --

- --ast-dump (finish implementation)

-- BIND --

- Fix some incomplete type pointers which are supposed to work
  (see incomplete-type-ptr-2.c test in bind/good)

-- TYPE CHECKER --

- Update error messages for return statement

-- ANALYSIS --

- Add volatile consideration for -Wuninitialized
- Add -Wunused-*

-- GEN --

- Fix some generation of casts (especially pointers to integers are no
  consistent)
