-- PARSE --

- Add C99 features
- Check strings/char/floats

-- AST --

- --ast-dump (dump the ast inner node on std::cout)

-- BIND --

-- TYPE CHECKER --

- Update error messages for return statement
- Handle case where function pointer receive a function (not other function
  pointer)

-- IR CONVERTER --

- Design IR

-- OPTIMISATION --

- ...

-- BACKEND --

- Add Target x86 32bits
- Add Target x86 64bits
- Add Target arm
- ...

-- OTHER --

- Correct locations starting at 0
- Add support of #line locations
- Fix handling of incomplete type pointers which are supposed to work
