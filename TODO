-- PARSE --

- Add C99 features
- Check strings/char/floats

-- AST --

- --ast-dump (dump the ast inner node on std::cout)

-- BIND --

-- TYPE CHECKER --

- Implement type checker
- Implement type classes

- Move actual_type() implem to Type class and let other classes decides to
  override it.

-- IR CONVERTER --

- Design IR

-- OPTIMISATION --

- ...

-- BACKEND --

- Add Target x86 32bits
- Add Target x86 64bits
- Add Target arm
- ...

-- OTHER --

- Correct locations starting at 0
- Add support of #line locations
